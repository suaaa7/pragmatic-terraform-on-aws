[
  {
    "name": "fargate",
    "image": "${repository_url}:${tag}",
    "cpu": ${cpu},
    "memory": ${memory},
    "networkMode": "awsvpc",
    "portMappings": [
      {
        "containerPort": 80,
        "protocol": "tcp"
      }
    ]
  }
]
