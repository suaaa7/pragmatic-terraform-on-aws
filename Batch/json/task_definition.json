[
  {
    "name": "fargate",
    "image": "${account_id}.dkr.ecr.${aws_region}.amazonaws.com/${repository}:${tag}",
    "cpu": ${cpu},
    "memory": ${memory},
    "networkMode": "awsvpc",
    "portMappings": [
      {
        "containerPort": 80,
        "protocol": "tcp"
      }
    ]
  }
]
